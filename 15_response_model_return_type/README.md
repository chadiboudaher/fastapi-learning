# Response Model

## What is Response Model?

`response_model` defines the shape, type, and structure of data your API returns. It acts as a filter, validator, and documentation generator.

## Why use it?

1. Security -> Hide sensitive fields
2. Validation -> Ensures data matches expected format
3. Conversion -> auto-convert type
