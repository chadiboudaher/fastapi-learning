# Query Parameters

1. In this lesson, I learned about functional parameters like skip, and limit that can be interpreted as query parameters.
2. Enabling a `None` option for a certain parmeter if a value is not set.
