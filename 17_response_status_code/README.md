# Response Status code

Status code are used in a lot of request body and response bodies. They are used to inform the developer of type of received message.

`status` is very code because it provide explanation on each code so we do not have to memorize them.
