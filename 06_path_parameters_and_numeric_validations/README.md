# Path Parameters and Numeric Validations

`Path` was introduced in this lesson.

1. it is located in the main part of the `URL`.
2. It is always mandatory.

We can declare numeric validations for both `Path` and `Query`:

- gt = greater than.
- ge = greater than or equal.
- lt = less than.
- le = less than or equal
