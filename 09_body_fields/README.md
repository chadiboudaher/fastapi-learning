# Body Fields

This is a class similar to `Body`, `Query`, and `Path` but it is import from `pydantic` not `fastapi`.

## What is `Field()`?

It validates data that comes from the request body. It is used from model validation.
