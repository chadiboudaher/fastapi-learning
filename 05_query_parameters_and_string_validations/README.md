# Query Parameters and String Validations

`Annotated` is better than `Query` for multiple reasons.

Generic validations and metadata:

- alias
- title
- description
- deprecated
- include_in_schema

Validations specific for strings:

- min_length
- max_length
- pattern

Custom validations using AfterValidator.
