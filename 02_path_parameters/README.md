# Path Parameters

This is part I learned about path parameters. They are used to send values/attr from page to page.

We learned about:

- `Enum` is a custom data type used to represent a fixed set of named constants.
- How to set a path as a parameter in a path.
