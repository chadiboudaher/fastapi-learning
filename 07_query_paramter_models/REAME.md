# Query Parameter Models

In this lesson, we explore the usage of a model as a query parameter. It can be used to help eliminate repetitiveness in the code. We can also a a forbid features with `model_config` to prevent the addition of undefined inputs/parameters.
